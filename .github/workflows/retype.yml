name: Publish Retype Docs
on:
  push:
    branches:
      - main
      - master

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.2.2 # Clears checkout warning
        with:
          fetch-depth: 0

      - name: Build website
        uses: retypeapp/action-build@v4.7.0 # Explicitly pinned runtime

      - name: Publish to Branch
        uses: retypeapp/action-github-pages@v4.7.0 # Explicitly pinned runtime
        with:
          update-branch: true